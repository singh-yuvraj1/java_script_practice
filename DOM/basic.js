// //DOM stands for document object model. It is a programming interface for web documents. It represents the page so that programs can change the document structure, style, and content. The DOM represents the document as nodes and objects; that way, programming languages can interact with the page. A web page is a document. This document can be either displayed in the browser window or as the HTML source. But it is the same document in both cases. The Document Object Model (DOM) is a cross-platform and language-independent interface that treats an XML or HTML document as a tree structure wherein each node is an object representing a part of the document.

// //dom is used to manipulate the html elements and attributes. It is used to change the content, structure, and style of a web page dynamically. The DOM allows developers to create interactive web applications by providing a way to access and modify the elements of a web page using JavaScript.

// The HTML DOM (Document Object Model) is a structured representation of a web page that allows developers to access, modify, and control its cont

// Structure of the HTML DOM
// Imagine your webpage as a tree

// The document is the root.
// HTML tags like <html>, <head>, and <body> are branches.

// Working of DOM
// The DOM connects your webpage to JavaScript, allowing you to:
// Access elements (like finding an <h1> tag).
// Modify content (like changing the text of a <p> tag).
// React to events (like a button click).
// Create or remove elements dynamically.



//---------------------dom methods---------------------

// getElementById(id) method is used to select an element by its unique ID.

// getElementsByClassName(class)

// getElementsByClassName(class) method retrieves all elements that share a given class name.

// querySelector(selector)

// querySelector(selector) method returns the first element that matches the specified selector.

// querySelectorAll(selector)

// querySelectorAll(selector) method selects all elements that match the given selector.

// createElement(tag)

// createElement(tag) method is used to create a new HTML element dynamically.

// appendChild(node)

// appendChild(node) method adds a child node to an existing element.

// remove()

// remove() method is called to remove an element from the DOM.

// addEventListener(event, fn)

// addEventListener(event, fn) method attaches an event handler function to an element.